# Changelog

## [Unreleased]

### Added

- Added the `pipi` agent MVP package with `AgentHarness` integration, `ROLE.md` prompt injection, session-scoped context tools, manual compaction, and isolation tests.
- Added the `pipi` CLI bin for print-mode prompts, llmbox environment integration, session context flags, and built-in default role fallback.
